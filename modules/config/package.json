{
  "name": "@lecstor/config",
  "private": true,
  "version": "1.0.0",
  "main": "dist/index.js",
  "license": "MIT",
  "files": [
    "dist"
  ],
  "types": "dist/index.d.ts",
  "scripts": {
    "update": "yarn build && yarn pack",
    "build": "rm -rf dist && tsc && chmod u+x ./dist/dump-config.js",
    "test": "echo",
    "dump-config": "node ./dist/dump-config.js"
  },
  "devDependencies": {
    "ts-node": "^8.6.2",
    "typescript": "^3.7.5"
  },
  "dependencies": {
    "copin": "^2.0.0",
    "lodash": "^4.17.15"
  }
}
