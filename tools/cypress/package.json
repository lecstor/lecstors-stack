{
  "name": "@lecstor/cypress",
  "version": "0.0.1",
  "license": "MIT",
  "scripts": {
    "open": "cypress open",
    "run": "cypress run",
    "report": "nyc report --reporter=json --reporter=lcov --reporter=text-summary",
    "test": "echo"
  },
  "devDependencies": {
    "@cypress/code-coverage": "^1.10.2",
    "cypress": "^4.0.2",
    "istanbul-lib-coverage": "^2.0.5",
    "nyc": "^14.1.1"
  }
}
